import './style.css'

const gameBoard = document.querySelector(".game-board"); // document method querySelector() return the first element within the document that matches the specified CSS selector.
const scoreElement = document.querySelector(".score");
const highScoreElement = document.querySelector(".highScore");